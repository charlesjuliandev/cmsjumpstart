# Decision

CMSJumpstart will provide built-in support for common authentication methods while preserving unrestricted custom header support. The library will not attempt to model every Drupal authentication architecture.