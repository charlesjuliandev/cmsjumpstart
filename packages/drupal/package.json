{
  "name": "@cmsjumpstart/drupal",
  "version": "0.1.0",
  "description": "Drupal integration for CMSJumpstart",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "pnpm clean && tsc",
    "typecheck:types": "tsc -p tsconfig.type-tests.json --noEmit"
  },
  "dependencies": {
    "@cmsjumpstart/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}

